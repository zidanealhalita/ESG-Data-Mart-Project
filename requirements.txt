# ESG Data Mart - Python Dependencies
#
# Instalasi:
#   pip install -r requirements.txt

pandas>=2.0.0          # ETL: membaca CSV & transformasi data
tabulate>=0.9.0        # (opsional) mencetak hasil query sebagai tabel markdown
graphviz>=0.20          # (opsional) meng-generate ulang diagram ERD di docs/
streamlit>=1.38.0       # Dashboard pelaporan interaktif (app/streamlit_app.py)
plotly>=5.22.0           # Grafik interaktif yang dipakai di dashboard Streamlit

# Catatan: modul 'sqlite3' adalah bagian dari Python standard library
# dan tidak perlu diinstal terpisah.
